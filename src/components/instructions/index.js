import React from "react";
import "./style.css";

function Instructions(props) {
  return <span id="instructions" className="instructions">Test your memory and click through each fat cat image without any repeats! Every time you click the images will re-shuffle. Try to reach a perfect score of 9!</span>;
}

export default Instructions;
